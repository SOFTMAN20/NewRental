# Netlify/Vercel SPA Redirects
# This ensures all routes go to index.html for client-side routing

# Serve index.html for all routes except static files
/*    /index.html   200

# Alternative format (more specific)
# /api/*  https://your-api-url.com/:splat  200
# /*      /index.html  200
